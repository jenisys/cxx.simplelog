# =====================================================
# INVOKE CONFIGURATION:
# =====================================================
# -- ON WINDOWS:
# run:
#   echo: true
#   pty:  false
#   shell: C:\Windows\System32\cmd.exe
# =====================================================
# MAYBE: tasks: auto_dash_names: false

project:
    name: cxx_log
    description: "Explore CXXLOG_...() macros w/ spdlog, ..."

run:
    echo: true
    pty:  true

clean:
    extra_directories:
      - "build"
      - "**/build"

clean_all:
    extra_directories:
      - .pytest_cache
      - bin

cmake:
    project_dirs:
      - .
      # DISABLED: - boost_log.example
    generator: ninja

